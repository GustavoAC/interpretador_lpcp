print "Digite um numero";
int a;
a = scan int;
print "Numero digitado: ", a;

[int] lista;
ptr [int] listaRef;
listaRef => lista;

int count;
count = 0;
while (count < a) do
	pushBack(listaRef, count);	
	count = count + 1;
endwhile


print "Tamanho da lista: ", len(lista), "/ Lista: ", lista;