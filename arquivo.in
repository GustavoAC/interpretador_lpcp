int a, i = 0;
a = scan int;

[int] lista;
ptr [int] listaRef;
listaRef => lista;

while (i < a) do
    pushBack(listaRef, i);
    i = i + 1;
endwhile

print "Tamanho da lista: ", len(lista), "/ Lista: ", lista;